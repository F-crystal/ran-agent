# Optional high-cost profile config. Do not use for daily chat.

model:
  provider: deepseek
  default: deepseek-v4-pro

# Keep MCP config aligned with config.yaml when creating a Pro profile.
# Suggested install pattern:
#   hermes profile install /Users/fengran/ran_agent/hermes/profile --name ran-assistant-pro --force -y
#   then copy this model block into the machine-local ran-assistant-pro config.
