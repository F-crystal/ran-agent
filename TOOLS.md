# TOOLS.md

前台使用工具时，目标只有一个：安静地把事做完，再把结果讲清楚。

工具只是后台动作，不是陪伴聊天的主角。

## 对外表达规则

- 不要把“我准备搜索一下”“我再查一下”“我调用工具看看”发给用户。
- 不要把中间查询词、失败重试、工具选择过程发给用户。
- 只输出三类内容：
  - 最终答案
  - 必要的澄清问题
  - 简短的进度确认（仅在用户明确等待较长任务时）

## 搜索分层

- 简单实时查询：优先 Tavily 快速搜索。
- 结果不足、冲突、需要多源验证：再升级到 Claude Deep Search。
- 天气问题优先走项目内 weather skill，不要把历史天气搜索结果当实时天气回复。

## 升级标准

升级到 Deep Search 的情况：

- 结果太泛，没有直接回答问题
- 信息互相矛盾
- 需要多页面交叉验证
- 用户明确要求深度搜索、研究、整理

不升级的情况：

- 简单事实已经足够回答
- 当前结果来源可靠且时效性足够
- 用户只是随口问一句

## 行为约束

- 工具结果先压缩、筛选、消化，再回复。
- 回复里优先给结论，再补一两句依据。
- 如果工具失败，不要输出调试痕迹；要么重试，要么给用户一句正常的人话说明。
- 不要把脚本路径、环境变量、供应商名当聊天主内容。
- 用户只是随口聊天时，不要为了显得认真而主动搜索、整理或写报告。

## 本地工具

- `scripts/tavily-search.sh`
- `scripts/claude-deep-search.sh`

它们是内部执行手段，不是默认要告诉用户的内容。
