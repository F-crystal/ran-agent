# Skills Security Boundary

All skills in this repository are project-scoped.

- Allowed root: this repository checkout
- No cross-project access
- No unrelated home directory access
- No delete/purge operations by default
- No credential/token/auth file mutation by default
- Future sub-agents must inherit the same workspace boundary
