# Agent Capability Governance

Status: CURRENT (2026-08-16)

## Three Layers

1. Skills are shared knowledge. Cross-tool skills live in `/Users/fengran/.agents/skills/<skill>`.
2. Hooks are executable lifecycle code. Keep them host-specific and record them in `/Users/fengran/.agents/hook-policy/`.
3. Plugins and MCP servers are capability entry points. Keep caches host-managed and record enabled entries in `/Users/fengran/.agents/plugin-inventory/`.

## Write Rules

- New cross-tool skill: create or edit `/Users/fengran/.agents/skills/<skill>/SKILL.md`. Prefer native discovery; add a per-skill adapter only for a runtime that lacks it.
- Project-only skill: keep it in the project `skills/<skill>/SKILL.md`.
- Do not maintain tool-specific copies of a shared skill.
- Do not symlink whole skill roots.
- Do not edit plugin cache directories by hand.
- Do not put credentials, cookies, API keys, session JSONL, SQLite state, logs, or local cache files in any governance inventory.

## New Projects

Every new project that adds `AGENTS.md`, `CLAUDE.md`, `GEMINI.md`, `.mcp.json`, `skills/`, `hooks/`, or plugin config must include a short Agent Capability Governance section:

```markdown
## Agent Capability Governance

- Cross-tool skills live in `/Users/fengran/.agents/skills`; project-only skills stay in this repo's `skills/`.
- Hooks, plugins, and MCP entries are executable capability surfaces; record new or changed entries in `/Users/fengran/.agents/hook-policy/` or `/Users/fengran/.agents/plugin-inventory/`.
- Do not edit tool-specific skill copies directly; use the shared source or a required per-skill adapter.
```

In this repo, `AGENTS.md` is the canonical root rule file and `CLAUDE.md` is only a shim to it. Do not maintain duplicate root rule sets.

## Current Exceptions

- `/Users/fengran/.codex/skills/.system`, Codex plugin caches, and bundled runtime skills are host-managed.
- `/Users/fengran/.codex/superpowers/skills` stays in the Superpowers plugin tree.
- `/Users/fengran/ran_agent/skills` and `/Users/fengran/ran_agent/hermes/profile/skills` are project/Hermes-specific.
- `/Users/fengran/.hermes/skills` is Hermes-specific.
- `/Users/fengran/.cc-switch/skills` is generated by cc-switch auto sync; do not treat it as canonical.

## Guardrails

- The canonical `agent-capability-governance` skill lives in `/Users/fengran/.agents/skills`.
- Global and project instructions are guidance, not a permission boundary. Rely on native sandboxing, approvals, protected paths, scoped credentials, and project trust for enforcement.
- The former Codex, Claude, and Cursor governance reminder hooks are retired; active host hooks remain recorded in `/Users/fengran/.agents/hook-policy/inventory.md`.
- This repo no longer ships the retired governance hook guard or installer. Its remaining `scripts/agent_project_governance_watch.py` is the source for the separately managed manual host audit at `/Users/fengran/.agents/bin/agent_project_governance_watch.py`; it requires explicit roots, does not follow symlinked directories, never patches project files, and exits nonzero when it finds an issue.
- The former periodic LaunchAgent is retired.
- The Time MCP launcher prefers a compatible preinstalled interpreter; its `uvx` fallback is bounded to a server/SDK pair with the required 1.x API.
- Inventory files use generated blocks; keep manual audit notes outside those blocks.
